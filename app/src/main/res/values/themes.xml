<?xml version="1.0" encoding="utf-8"?>
<!-- res/values/styles.xml -->
<resources>
    <!-- Requires com.google.android.material:material (or use AppCompat parent if you prefer) -->
    <style name="Theme.DadJokes" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">false</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item> <!-- white icons -->
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>

